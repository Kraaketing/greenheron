<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="description"
      content="Custom-designed, low-impact websites from Green Heron Design. Built with minimal, efficient code and hosted on eco-friendly servers for a more sustainable web."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/monogramdot.svg">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body class="push-footer | background--light-orange"  id="top">
    <div class="standard-container container-width-full height-flex-grow">
      {% include "components/header.njk" %}
      <main class="standard-container height-flex-grow">
        {% if content %}
          <section class="container-width-narrow | info">
              {{ content | safe }} 
          </section>
        {% endif %}

        {% if blocks %} {% for block in blocks %} 

        {% if block.type == "hero" %}
        {% include "components/hero.njk" %} 

        {% elif block.type == "intro" %} 
        {% include "components/intro.njk" %} 

        {% elif block.type == "mountainwave" %} 
        {% include "components/mountainwave.njk" %} 

        {% elif block.type == "testimonials" %} 
        {% include "components/testimonials.njk" %} 

        {% elif block.type == "contact-block" %} 
        {% include "components/contact-block.njk" %} 
        {% endif %} 
        {% endfor %}
        {% endif %}
      </main>
    </div>
    {% include "components/footer.njk" %}
  </body>
</html>