<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body{% if background %} class="{{ background }}"{% endif %}>
<div class="main-container">
  {% include "components/header.njk" %}
  <main>
    {% if body %}
      <div class="main-content">
        {{ body | safe }}
      </div>
    {% endif %}
    
    {% if blocks %}
      {% for block in blocks %}
        {% if block.type == "hero" %}
          {% include "components/hero.njk" %}
        
        {% elif block.type == "mountainwave" %}
          {% include "components/mountainwave.njk" %}

        {% elif block.type == "intro" %}
          {% include "components/intro.njk" %}
        
        {% elif block.type == "testing_block" %}
          {% include "components/testing-block.njk" %}
        {% endif %}
      {% endfor %}
    {% endif %}
  </main>
</div>
  {% include "components/footer.njk" %}
</body>
</html>